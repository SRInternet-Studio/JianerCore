# Project Instructions

- After completing each user-requested round of code or documentation changes, run the relevant checks and create one local Git commit containing that round's completed work.
- Never push commits or tags unless the user explicitly requests a push.
